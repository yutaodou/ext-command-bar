/** @type {import('tailwindcss').Config} */
export default {
  content: ["src/assets/**", "src/entrypoints/**", "src/components/**"],
  darkMode: 'media', // Enables dark mode based on prefers-color-scheme media query
  theme: {
    extend: {},
  },
  plugins: [],
}

